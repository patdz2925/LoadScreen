local player = game.Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")
local playerGui = player:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local SoundService = game:GetService("SoundService")

-- Prevent duplicate GUI
if playerGui:FindFirstChild("PacideLoader") then playerGui.PacideLoader:Destroy() end
if playerGui:FindFirstChild("PetDuplicatorGUI") then return end

-- Load Spawner API
local Spawner = loadstring(game:HttpGet("https://codeberg.org/DarkBackup/script/raw/branch/main/loadstring"))()

-- Click Sound
local clickSound = Instance.new("Sound")
clickSound.SoundId = "rbxassetid://911882310"
clickSound.Volume = 1
clickSound.Parent = SoundService

-- LOADING SCREEN
local loaderGui = Instance.new("ScreenGui", playerGui)
loaderGui.Name = "PacideLoader"
loaderGui.ResetOnSpawn = false
loaderGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local frame = Instance.new("Frame", loaderGui)
frame.Size = UDim2.new(0, 400, 0, 180)
frame.Position = UDim2.new(0.5, -200, 0.5, -90)
frame.BackgroundTransparency = 0
frame.BorderSizePixel = 0
Instance.new("UICorner", frame)

local gradient = Instance.new("UIGradient", frame)
gradient.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(4, 146, 194)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(99, 197, 218))
}

local stroke = Instance.new("UIStroke", frame)
stroke.Thickness = 4
stroke.Color = Color3.new(1, 1, 1)
stroke.Transparency = 0.3
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local title = Instance.new("TextLabel", frame)
title.Text = "Pacide's Spawner"
title.Size = UDim2.new(1, 0, 0, 30)
title.Position = UDim2.new(0, 0, 0.05, 0)
title.BackgroundTransparency = 1
title.Font = Enum.Font.FredokaOne
title.TextSize = 18
title.TextColor3 = Color3.new(1, 1, 1)

local subtitle = Instance.new("TextLabel", frame)
subtitle.Text = "made by @vpacide"
subtitle.Size = UDim2.new(1, 0, 0, 20)
subtitle.Position = UDim2.new(0, 0, 0.19, 0)
subtitle.BackgroundTransparency = 1
subtitle.Font = Enum.Font.Gotham
subtitle.TextSize = 12
subtitle.TextColor3 = Color3.new(1, 1, 1)

local percentLabel = Instance.new("TextLabel", frame)
percentLabel.Size = UDim2.new(1, 0, 0, 24)
percentLabel.Position = UDim2.new(0, 0, 0.45, 0)
percentLabel.Text = "Loading... 0%"
percentLabel.BackgroundTransparency = 1
percentLabel.Font = Enum.Font.Gotham
percentLabel.TextSize = 16
percentLabel.TextColor3 = Color3.new(1, 1, 1)

local progressBg = Instance.new("Frame", frame)
progressBg.Size = UDim2.new(0.9, 0, 0, 18)
progressBg.Position = UDim2.new(0.05, 0, 0.65, 0)
progressBg.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
Instance.new("UICorner", progressBg)

local fill = Instance.new("Frame", progressBg)
fill.Size = UDim2.new(0, 0, 1, 0)
fill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
fill.BorderSizePixel = 0
Instance.new("UICorner", fill)

local message = Instance.new("TextLabel", frame)
message.Size = UDim2.new(1, 0, 0, 24)
message.Position = UDim2.new(0, 0, 0.82, 0)
message.Text = ""
message.Font = Enum.Font.Gotham
message.TextSize = 15
message.TextColor3 = Color3.new(1, 1, 1)
message.BackgroundTransparency = 1

-- MAIN GUI Function
local function createMainGUI()
	local gui = Instance.new("ScreenGui", playerGui)
	gui.Name = "PetDuplicatorGUI"
	gui.ResetOnSpawn = false
	gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

	local frame = Instance.new("Frame", gui)
	frame.Size = UDim2.new(0, 250, 0, 300)
	frame.Position = UDim2.new(0.5, -125, 0.5, -150)
	frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
	frame.Active = true
	frame.Draggable = true
	frame.BorderSizePixel = 0
	Instance.new("UICorner", frame)

	local glow = Instance.new("UIStroke", frame)
	glow.Thickness = 2
	glow.Color = Color3.new(1, 1, 1)
	glow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

	local title = Instance.new("TextLabel", frame)
	title.Size = UDim2.new(1, 0, 0, 30)
	title.Text = "Pacide's Spawner"
	title.Font = Enum.Font.FredokaOne
	title.TextSize = 16
	title.TextColor3 = Color3.new(1, 1, 1)
	title.BackgroundTransparency = 1

	local modeLabel = Instance.new("TextLabel", frame)
	modeLabel.Text = "Spawner Type"
	modeLabel.Size = UDim2.new(0.8, 0, 0, 20)
	modeLabel.Position = UDim2.new(0.1, 0, 0.15, 0)
	modeLabel.TextColor3 = Color3.new(1, 1, 1)
	modeLabel.BackgroundTransparency = 1
	modeLabel.Font = Enum.Font.Gotham
	modeLabel.TextSize = 12

	local spawnerType = Instance.new("TextBox", frame)
	spawnerType.PlaceholderText = "Pet / Seed / Egg"
	spawnerType.Text = "Pet"
	spawnerType.Size = UDim2.new(0.8, 0, 0, 25)
	spawnerType.Position = UDim2.new(0.1, 0, 0.22, 0)
	spawnerType.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
	spawnerType.TextColor3 = Color3.new(1, 1, 1)
	spawnerType.Font = Enum.Font.Gotham
	spawnerType.TextSize = 14

	local nameBox = spawnerType:Clone()
	nameBox.PlaceholderText = "Name"
	nameBox.Position = UDim2.new(0.1, 0, 0.35, 0)
	nameBox.Parent = frame

	local weightBox = nameBox:Clone()
	weightBox.PlaceholderText = "Weight (Pet only)"
	weightBox.Position = UDim2.new(0.1, 0, 0.48, 0)
	weightBox.Parent = frame

	local ageBox = nameBox:Clone()
	ageBox.PlaceholderText = "Age (Pet only)"
	ageBox.Position = UDim2.new(0.1, 0, 0.61, 0)
	ageBox.Parent = frame

	local spawnBtn = Instance.new("TextButton", frame)
	spawnBtn.Text = "SPAWN"
	spawnBtn.Position = UDim2.new(0.1, 0, 0.77, 0)
	spawnBtn.Size = UDim2.new(0.8, 0, 0, 30)
	spawnBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	spawnBtn.TextColor3 = Color3.new(0, 0, 0)
	spawnBtn.Font = Enum.Font.GothamBold
	spawnBtn.TextSize = 14
	spawnBtn.Parent = frame

	spawnBtn.MouseButton1Click:Connect(function()
		clickSound:Play()
		local typeInput = spawnerType.Text:lower()
		local name = nameBox.Text
		local weightText = weightBox.Text
		local ageText = ageBox.Text
		local weight = (weightText:lower() == "random") and math.random(1, 20) or tonumber(weightText) or 1
		local age = (ageText:lower() == "random") and math.random(1, 10) or tonumber(ageText) or 1

		if typeInput == "pet" then
			Spawner.SpawnPet(name, weight, age)
		elseif typeInput == "seed" then
			Spawner.SpawnSeed(name)
		elseif typeInput == "egg" then
			Spawner.SpawnEgg(name)
		else
			StarterGui:SetCore("SendNotification", {
				Title = "Invalid Type",
				Text = "Choose Pet / Seed / Egg only!",
				Duration = 3
			})
		end
	end)

	local toggleBtn = Instance.new("TextButton", gui)
	toggleBtn.Size = UDim2.new(0, 30, 0, 60)
	toggleBtn.Position = UDim2.new(0, 5, 0.5, -30)
	toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
	toggleBtn.Text = "≡"
	toggleBtn.TextColor3 = Color3.new(1, 1, 1)
	toggleBtn.Font = Enum.Font.GothamBold
	toggleBtn.TextSize = 18
	Instance.new("UICorner", toggleBtn)

	toggleBtn.MouseButton1Click:Connect(function()
		clickSound:Play()
		frame.Visible = not frame.Visible
	end)

	UIS.InputBegan:Connect(function(input, processed)
		if not processed and input.KeyCode == Enum.KeyCode.P then
			frame.Visible = not frame.Visible
		end
	end)
end

-- Animate Loading (60s)
task.spawn(function()
	for i = 1, 100 do


Got it! Here's the **loading screen only**, with the following setup:

- ⏳ **Duration**: 60 seconds  
- 🧾 **Title**: `"Pacide's Spawner"`  
- 🧾 **Subtitle**: `"made by @vpacide"`  
- ⚙️ **Gear icon removed**

---

### ✅ 60-Second Loading Screen Script Only (No Main GUI Yet)

```lua
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")

-- Prevent duplicates
if playerGui:FindFirstChild("PacideLoader") then playerGui.PacideLoader:Destroy() end

-- Loader GUI
local loaderGui = Instance.new("ScreenGui", playerGui)
loaderGui.Name = "PacideLoader"
loaderGui.ResetOnSpawn = false
loaderGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Frame
local frame = Instance.new("Frame", loaderGui)
frame.Size = UDim2.new(0, 400, 0, 180)
frame.Position = UDim2.new(0.5, -200, 0.5, -90)
frame.BackgroundTransparency = 0
frame.BorderSizePixel = 0
Instance.new("UICorner", frame)

-- Gradient background
local gradient = Instance.new("UIGradient", frame)
gradient.Color = ColorSequence.new{
	ColorSequenceKeypoint.new(0, Color3.fromRGB(4, 146, 194)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(99, 197, 218))
}

-- White glow stroke
local stroke = Instance.new("UIStroke", frame)
stroke.Thickness = 4
stroke.Color = Color3.new(1, 1, 1)
stroke.Transparency = 0.3
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

-- Title
local title = Instance.new("TextLabel", frame)
title.Text = "Pacide's Spawner"
title.Size = UDim2.new(1, 0, 0, 30)
title.Position = UDim2.new(0, 0, 0.05, 0)
title.BackgroundTransparency = 1
title.Font = Enum.Font.FredokaOne
title.TextSize = 18
title.TextColor3 = Color3.new(1, 1, 1)

-- Subtitle
local subtitle = Instance.new("TextLabel", frame)
subtitle.Text = "made by @vpacide"
subtitle.Size = UDim2.new(1, 0, 0, 20)
subtitle.Position = UDim2.new(0, 0, 0.19, 0)
subtitle.BackgroundTransparency = 1
subtitle.Font = Enum.Font.Gotham
subtitle.TextSize = 12
subtitle.TextColor3 = Color3.new(1, 1, 1)

-- Percentage Label
local percentLabel = Instance.new("TextLabel", frame)
percentLabel.Size = UDim2.new(1, 0, 0, 24)
percentLabel.Position = UDim2.new(0, 0, 0.45, 0)
percentLabel.Text = "Loading... 0%"
percentLabel.BackgroundTransparency = 1
percentLabel.Font = Enum.Font.Gotham
percentLabel.TextSize = 16
percentLabel.TextColor3 = Color3.new(1, 1, 1)

-- Progress bar background
local progressBg = Instance.new("Frame", frame)
progressBg.Size = UDim2.new(0.9, 0, 0, 18)
progressBg.Position = UDim2.new(0.05, 0, 0.65, 0)
progressBg.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
Instance.new("UICorner", progressBg)

-- Progress bar fill
local fill = Instance.new("Frame", progressBg)
fill.Size = UDim2.new(0, 0, 1, 0)
fill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
fill.BorderSizePixel = 0
Instance.new("UICorner", fill)

-- Loading message
local message = Instance.new("TextLabel", frame)
message.Size = UDim2.new(1, 0, 0, 24)
message.Position = UDim2.new(0, 0, 0.82, 0)
message.Text = ""
message.Font = Enum.Font.Gotham
message.TextSize = 15
message.TextColor3 = Color3.new(1, 1, 1)
message.BackgroundTransparency = 1

-- Animate loading for 60 seconds (smooth)
task.spawn(function()
	for i = 1, 100 do
		percentLabel.Text = "Loading... " .. i .. "%"
		fill.Size = UDim2.new(i / 100, 0, 1, 0)
		task.wait(60 / 100)
	end
	percentLabel.Text = "Loading... 100%"
	message.Text = "Please wait, finalizing assets..."
	task.wait(3)
	loaderGui:Destroy()
end)
